{
  "name": "agent-lens-workspace",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "dev": "pnpm --filter @agent-lens/server dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "check:entry-size": "node scripts/check-entry-size.mjs",
    "check:entry-size:strict": "node scripts/check-entry-size.mjs --strict",
    "release:check": "pnpm typecheck && pnpm check:entry-size && pnpm -r build && (cd packages/server && pnpm pack && rm -f *.tgz) && (cd packages/ui && pnpm pack && rm -f *.tgz) && (cd packages/cli && pnpm pack && rm -f *.tgz)"
  },
  "devDependencies": {
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
